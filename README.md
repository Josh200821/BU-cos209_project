
classes are used to encapsulate data and functions while creating objects

arrow functions provide a shorter syntax for writing functions instead of using the return function

variables are names, words or alphabets used to store data.
demo vars used implies demonstration variables

built in functions in js to make use of arrrays easier

Destructuring is a shorter and easier way to pull out values
 from objects or arrays and store them in variables.
 person takes the 'name' and 'job'
 values from the 'person' object and saves them as variables
 num takes the first two values from the
   'num' array and stores them in 'x' and 'y'.

modules are files that contain code that can be imported and used in other files.
code written here can be used in other files by importing the function or variable used

tenary - it is used to shorten if-else statements

spread.js
lets you expand arrays by adding a previously declared array into a new array 

the index.js folder contains the code to be imported and the import path to be used and the way the code should run.
